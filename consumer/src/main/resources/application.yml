server.port: 8081
kafka.consumer:
  bootstrap.servers: localhost:9092
  group.id: group1
  concurrency: 1
  autoOffsetReset: earliest
  maxPollRecords: 5
  maxPollIntervalMs: 15000
  metadataMaxAgeMs: 15000
  topic: demo
  delay: 2000
logging:
  file: application-${PID}.log